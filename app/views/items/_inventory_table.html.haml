%table#inventory-table{ data: {sort_by: @sort_by, sort_type: @sort_type} }
  %thead
    %tr
      %th.sorter#name-header
        %span Item
        = inline_svg "sort.svg", class: "sort-hide"
      %th.sorter#quantity-header
        %span Quantity
        = inline_svg "sort.svg", class: "sort-hide"
      %th.sorter#price-header
        %span Price
        = inline_svg "sort.svg", class: "sort-hide"
      %th.sorter#kind-header
        %span Kind
        = inline_svg "sort.svg", class: "sort-hide"
      %th.sorter#status-header
        %span Status
        = inline_svg "sort.svg", class: "sort-hide"
      %th.add-column Add
  %tbody
    - @items.each do |item|
      %tr
        %td= link_to_function item.name, "showItem(#{item.id})", class: "inventory-table-link", id: "edit-#{item.name.parameterize}"
        %td= item.quantity
        %td= item.price 
        %td= item.kind
        %td= item.status
        %td.add-column
          %a{ href: "#", id: "add-#{item.name.parameterize}", onclick: "updateCart(#{item.id});" }
            = inline_svg "plus.svg", class: "add-svg"